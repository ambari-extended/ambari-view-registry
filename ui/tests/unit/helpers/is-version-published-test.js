import { isVersionPublished } from 'ui/helpers/is-version-published';
import { module, test } from 'qunit';

module('Unit | Helper | is version published');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isVersionPublished([42]);
  assert.ok(result);
});
